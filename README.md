# Kalshi clipper backup

Private backup of Neel's Kalshi paper-then-tiny-live clipper (`ws_clip.py`, `scan_lib.py`, `supervise.sh`).

Keys live outside this repo (`~/.config/kalshi/`). Never commit PEM, key_id, logs, or runtime status.

Not a public bot. Poly parked. No 5-min crypto.
